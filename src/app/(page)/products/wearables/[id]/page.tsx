import { DetailComputer } from "@/app/Components/Detail/DetailProduct"

export default function DetailWearables () {
    const category = "wearables"
                return (
                    <>
                        <DetailComputer category={category} />
                    </>
                )
}